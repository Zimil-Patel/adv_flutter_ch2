import 'package:flutter/material.dart';

class BottomIconsModel {
  IconData activeIcon, unactiveIcon;
  String text;

  BottomIconsModel(
      {required this.activeIcon,
      required this.unactiveIcon,
      required this.text});
}
